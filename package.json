{
  "name": "rhythm-guitar",
  "version": "0.1.0",
  "description": "chord positions for a midi rhythm guitar",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "spago build",
    "test": "spago test",
    "example": "spago bundle --package example --outfile dist/app.js --bundle-type app --platform browser"
  },
  "devDependencies": {
    "purescript": "^0.15.15",
    "purescript-psa": "^0.6.0",
    "spago": "^0.93.42"
  },
  "author": "John Watson",
  "license": "MIT",
  "dependencies": {
    "big-integer": "^1.6.51",
    "esbuild": "^0.18.20"
  }
}
